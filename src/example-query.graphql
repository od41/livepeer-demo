# Transcoding Jobs Query
query ExampleQuery {
  protocols(first: 5) {
    id
    inflation
    inflationChange
    numActiveTranscoders
  }
  transcoders(first: 5) {
    id
    activationRound
    deactivationRound
    lastActiveStakeUpdateRound
  }
}